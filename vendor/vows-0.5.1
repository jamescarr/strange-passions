#!/usr/local/bin/node
// generated by npm, please don't touch!
var depMet = require.paths.indexOf("vendor/.npm/vows/0.5.1/dependencies") !== -1
if (!depMet) require.paths.unshift("vendor/.npm/vows/0.5.1/dependencies")
module.exports = require("vendor/.npm/vows/0.5.1/package/bin/vows")
if (!depMet) {
  var i = require.paths.indexOf("vendor/.npm/vows/0.5.1/dependencies")
  if (i !== -1) require.paths.splice(i, 1)
}